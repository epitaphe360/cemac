-- Production-safe seed entry point.
--
-- Automatic seeding is disabled in config.toml. This file intentionally
-- contains no identities, passwords, API credentials, or business records.
-- Local sample data is explicitly opt-in; see seeds/README.md.

SELECT 'No automatic seed configured' AS notice;
